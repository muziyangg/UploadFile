{
  "name": "github-file-uploader",
  "version": "1.0.0",
  "description": "A file uploader that uploads files to GitHub repository via Vercel",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "vercel": "^28.4.16"
  }
}
